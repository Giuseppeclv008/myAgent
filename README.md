# Tutorial: Creazione del primo Agente AI con LangChain e OpenRouter

Questo documento guida alla configurazione di un ambiente Python per l'utilizzo di agenti basati su modelli di linguaggio (LLM) tramite l'infrastruttura di OpenRouter.

---

## 1. Configurazione Ambiente Virtuale
Un ambiente virtuale isola i pacchetti del progetto evitando conflitti.

```bash
# Creazione dell'ambiente
python3 -m venv venv

# Attivazione (macOS/Linux)
source venv/bin/activate

# Attivazione (Windows)
# venv\Scripts\activate